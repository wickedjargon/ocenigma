import std::sv::SV
import @movegen
import @board::{Board, Position}
import @bitboards::{BitBoards, Piece}
import @movegen::PieceColorAgnostic

def main(argc: i32, argv: &str) {
    let board = Board::new_game()!!
    board.print_board()
}
